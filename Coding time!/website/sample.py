#from website import create_app, db
#from website.models import User, Task

# Create the app
#app = create_app()

#with app.app_context():
    # Create a sample user
    #user = User(username="user", email="generic@email.com", password="password")

    # Create a sample task and associate it with the user
    #task = Task(details="Finish project", duration=2, priority="High", difficulty="Medium", user=user)

    # Add the user and the task to the session
   # db.session.add(user)
    #db.session.add(task)

    # Commit the changes to the database
    #db.session.commit()

    #print("Sample data added successfully.")
